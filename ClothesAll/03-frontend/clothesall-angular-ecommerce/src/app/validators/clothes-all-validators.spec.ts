import { ClothesAllValidators } from './clothes-all-validators';

describe('ClothesAllValidators', () => {
  it('should create an instance', () => {
    expect(new ClothesAllValidators()).toBeTruthy();
  });
});
